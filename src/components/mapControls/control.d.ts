/**
 * Map Control type used by redux
 *
 * @author Neeraj Patel
 */

export default interface controlType {
  landmarks: {
    REST: boolean;
    CAFE: boolean;
    BAR_: boolean;
    EVNT: boolean;
  };
}
